name: Temporary AttendGate Runner Check

on:
  pull_request:
    branches: [ main ]

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Confirm runner
        run: echo 'Public GitHub Actions runner is available'
